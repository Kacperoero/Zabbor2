export interface Equipment {
    boots: string | null;      // Buty
    armor: string | null;      // Zbroja
    gloves: string | null;     // Rękawice
    helmet: string | null;     // Nakrycie głowy
    rightHand: string | null;  // Broń w prawej ręce
    leftHand: string | null;   // W lewej ręce (tarcza/broń)
    necklace: string | null;   // Naszyjnik
}
